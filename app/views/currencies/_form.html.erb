<%= form_with(model: currency, local: true) do |form| %>
  <% if currency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(currency.errors.count, "error") %> prohibited this currency from being saved:</h2>

      <ul>
      <% currency.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :abbr %>
    <%= form.text_field :abbr, id: :currency_abbr %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :currency_description %>
  </div>

  <div class="field">
    <%= form.label :is_base %>
    <%= form.check_box :is_base, id: :currency_is_base %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
